/**
* didn't make signin services yet: the logic needed is minimal for this module but
* some information might be needed across the app
*/

angular.module('turntify.signin', [])
.controller('SigninController', function ()/*probably requires 'UserService' from services*/ {
  //display logic for login button (for now)
});
