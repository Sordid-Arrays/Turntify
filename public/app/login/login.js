/**
* didn't make login services yet: the logic needed is minimal for this module but
* some information might be needed across the app
*/

angular.module('turntify.login', [])
.controller('LoginController', function (UserService)/*probably requires 'UserService' from services*/ {
});
